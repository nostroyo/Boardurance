# Tasks

- [x] 1. Add the "Specs" gate to CLAUDE.md Definition of done (ci-cd/Spec validation gate)
- [x] 2. Verify gate: `openspec validate --all --strict` passes on this branch (ci-cd/Spec validation gate)
